import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Prep = () => {
  return (
    <div className="container mx-auto p-6">
      <Card>
        <CardHeader>
          <CardTitle>PREP</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Conteúdo sobre PREP será adicionado aqui.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default Prep;